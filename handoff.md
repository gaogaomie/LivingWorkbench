# 日常集项目交接

更新时间：2026-09-03

## 最新进展

- Web 已接入 Tailwind CSS 4，应用壳、桌面侧栏和移动导航改用 utility class；原 `app.css` 已按基础、总览、登录、财务、业务模块和时间线拆分，不再由单文件承载全部样式。
- Web 请求层已统一为 Axios 实例，并按业务域拆分 `services` 与 React Query hooks；Cookie、API Base URL、错误映射和 CSRF Header 只在统一入口维护。
- 已补 React Testing Library 组件测试和 Playwright 关键 E2E，覆盖 Toast live region、移动导航键盘交互和登录表单可访问名称。
- 已增加 Web 源文件 600 行门禁；当前最大 Web 源文件为 583 行。
- 表单字段已补齐可访问名称，Toast 增加读屏 live region，应用增加跳到主要内容链接；移动菜单支持 `aria-expanded`、Escape 关闭和焦点归还。
- 管理员登录已由用户验证通过。凭据明文不写入源码、README 或交接文档。
- 已完成“时光档案、习惯健康、减脂健身、日程统筹、待买清单、书影音”六个模块的 MVP 纵向切片。
- 时光档案不复制业务数据，而是从财务、习惯、健身、日程、待买和书影音实时投影统一事件流，支持日期与来源筛选。
- 今日总览已接入真实习惯、待办和跨模块最近记录，并启用对应快捷入口。
- 今日总览已改为单一 `/api/v1/overview` 聚合接口，一次读取财务、习惯、待办和最近记录。
- 时光档案已改为不透明游标分页，默认每页 20 条、接口最多 50 条，排序增加稳定 ID 兜底，翻页不会重复。
- 财务、习惯、健身记录、待办、待买和书影音统一接入软删除；删除前使用 Animal Island Modal 二次确认。
- 设置页已提供“最近删除”，最多列出最近 50 条，支持恢复；删除和恢复均校验时间戳，冲突返回 409。
- 习惯、健身记录、待办、待买和书影音已支持完整编辑；原有新增表单会切换到编辑模式并回填数据。
- 五类更新接口统一使用 `expectedUpdatedAt` 乐观并发控制，陈旧编辑返回 409；编辑成功、失败和冲突使用 Toast 反馈。
- 前端错误反馈已统一为顶部 Toast：请求读取失败、提交失败、并发冲突和表单业务校验不再在页面内渲染错误块。
- Toast 由 QueryClient 全局错误入口和 Animal Island `Notification` 共同承载，401 登录态失效不重复弹窗，同文案短时间内自动去重。
- 习惯健康已增加近 30 天真实完成热力图；服务端统一返回每日计划数、完成数和完成率，前端只负责呈现。
- 财务页已增加本月支出分类图，直接使用服务端 `categoryBreakdown` 展示金额、占比和相对规模。
- 健身页已增加最近 14 次有效体重趋势，自动适配当前体重区间；无体重记录时提供空状态。
- 日程页已增加周一至周日的七天视图，合并全天与定时事项，标识今天与已完成事项。
- 日程提醒已完成一期闭环：支持到时、提前 10/30/60 分钟和提前 1 天；无具体时间时服务端和前端都会拒绝开启提醒。
- 已增加鉴权后的到期提醒查询，工作台打开时每分钟轻量检查一次，以站内 Toast 提醒；已完成、已取消和已删除事项不会触发。
- 数据安全页已改为业务全量 Excel 导出，提供导出说明以及财务、习惯、健身、日程、待买、书影音六个可读工作表；密码、Session、AI 密钥和封面文件明确排除。
- Excel 内置不可见、受保护的恢复数据工作表，按单元格长度限制分片保存版本化载荷；同一个 `.xlsx` 文件可直接用于恢复预检和事务恢复，不再要求用户管理 JSON 文件。
- 备份格式包含格式版本、应用版本、Schema 版本、导出时间、实体计数和 SHA-256；恢复预检会检查校验和、计数、引用关系、重复 ID 与业务唯一约束。
- 恢复必须经过文件预检和 Animal Island Modal 二次确认；服务端先写恢复前快照，再在单个 SQLite 事务中整体替换当前用户业务数据，管理员和 Session 不受影响。
- 书影音封面链路已完成：支持 JPG、PNG、WebP，客户端和服务端均限制 5 MB；服务端校验真实图片内容、限制解码像素、纠正方向、缩放至最大 900×1200 并统一压缩为 WebP。
- 封面读取按管理员和作品关联鉴权，使用私有长缓存与 ETag；替换、移除和 Excel 整体恢复会清理对应资源记录与文件，软删除作品时保留封面以支持回收站恢复。
- 书影音编辑器已增加 Animal Island 风格封面预览、选择、更换和“保存时移除”操作；列表封面采用懒加载，所有失败继续使用 Toast 反馈。
- 所有 JSON API 已统一为且只允许 `code`、`message`、`data` 三个顶层字段；空结果、错误、Excel 和封面内容也已纳入统一响应，并增加服务端运行时守卫。
- 当前 `bun run check` 全量通过：15 个测试文件、38 个测试通过，Lint、Web 文件尺寸、共享包/Web/服务端类型检查以及三端生产构建均通过。`bun run test:e2e` 为 3 个通过、1 个按项目条件跳过。

## 当前状态

项目方向、范围和架构以 [`docs/PRD-日常集生活工作台.md`](./docs/PRD-日常集生活工作台.md) 与 [`docs/技术方案-日常集生活工作台-Web一期.md`](./docs/技术方案-日常集生活工作台-Web一期.md) 为准，启动方式见 [`README.md`](./README.md)。

已完成：

- Bun monorepo、React Web、Fastify API、共享 Zod 合约、SQLite/Drizzle 数据层。
- 单管理员初始化、Argon2id 密码、签名 HttpOnly Session Cookie、CSRF 与来源校验。
- 财务 Repository 接口及 SQLite 实现；账目创建、查询、修改、软删除，月度预算与月度汇总。
- 账目金额按整数分存储；客户端 UUID 提供幂等创建；更新时间提供乐观并发控制。
- 习惯创建、每日进度、暂停/恢复、连续天数与近 30 天统计。
- 习惯近 30 天完成热力图，含无计划日、分级完成度和移动端布局。
- 财务分类图、最近 14 次体重趋势和本周七天日程视图，全部复用现有接口数据，无额外请求瀑布。
- 日程提醒设置、跨天到期时间计算、到期查询和应用内 Toast；轮询只在已登录布局中运行。
- 版本化 Excel 业务备份、下载、10 MB 文件预检、恢复摘要、二次确认、恢复前快照和事务恢复。
- 健身档案、每日体重/运动记录、BMI、目标进度与近 7 天均值。
- 默认生活清单、待办创建、完成/恢复、今日/逾期/未来 7 天统计。
- 待买物品创建、待买/已购状态流转、预计预算与本月购买统计。
- 书影音条目创建、在看/完成状态流转、年度完成数、评分和类型分布。
- 书影音封面上传、服务端 WebP 压缩、鉴权读取、预览、懒加载、替换、移除与文件清理。
- Animal Island 风格的登录、总览与全部业务页面，含桌面和移动响应式布局。
- 统一回收站、恢复能力、删除确认和跨页面缓存刷新。
- 五类生活记录的完整编辑表单、更新 API 与并发冲突保护。
- 前端全局 Toast 错误治理，页面不再内嵌登录、查询、提交或并发冲突错误。
- API 响应相关回归检查及整站检查全绿：15 个测试文件、38 个测试；共享包、Web 和服务端类型检查与生产构建均通过。

## 首轮检查结论（2026-09-03）

修复优先级：

1. 高：除登录页外，业务页 `FormItem` 均未设置 `name`，Animal Island 组件因此不会生成 `label htmlFor` 和控件 `id`；视觉标签没有与 Input、Select、DatePicker 等控件建立程序化关联。
2. 高：全局错误和业务状态都使用 Animal Island 命令式 `Notification`，但当前组件实现没有 `aria-live`、`role="status"` 或 `role="alert"`，屏幕阅读器无法获知 Toast 更新。
3. 中：应用布局没有“跳到主要内容”链接；移动菜单按钮缺少 `aria-expanded`、`aria-controls`，并且没有 Escape 关闭和开关后的焦点管理。
4. 中：封面文件删除失败会被静默吞掉，且整体恢复会先删除资源记录再清文件；一旦清理失败，孤儿文件既没有日志，也没有可追踪的补偿任务。
5. 中：封面上传超限通过匹配英文错误消息识别；`@fastify/multipart` 已提供稳定的 `RequestFileTooLargeError` 类型，应改为类型判断。
6. 中：Web 端没有组件或端到端自动化测试，表单标签、Toast、移动导航、封面和备份关键流程没有回归保护。
7. 低：多处图片依赖 CSS 尺寸但未声明 HTML `width`/`height`；业务筛选状态没有同步到 URL；编辑中离开页面没有未保存提醒。
8. 性能：Web 主 chunk 为 565.35 kB（gzip 181.30 kB），仍触发 Vite 500 kB 警告；三套简体中文字体资源合计约 3.47 MB。

依赖安全扫描尚无有效结论：`bun audit` 的远端接口返回 404，`bun pm scan` 又要求先配置第三方 scanner；这两项都不是“未发现漏洞”，后续需要选定可靠扫描器或在 CI 接入其他审计方式。

## 产品经理审判与补全清单（2026-09-03）

结论：当前已经是可真实使用的个人生活工作台 MVP，但还不是成熟完成品。功能完整度约 7.5/10、易用性约 6.3/10、数据可信度约 6.0/10、发布成熟度约 5.0/10，综合约 6.6/10。模块数量已经足够，下一阶段应优先补产品信任、记录效率、回顾能力与发布治理，不宜继续平铺大量孤立模块。

### P0：正式发布前完成

- [ ] 修复“全量备份”与封面恢复风险。当前 Excel 不包含封面二进制文件，整体恢复会把书影音记录的 `coverAssetId` 置空并清理现有封面。优先把封面纳入可恢复备份；若暂不实现，必须在导出说明、预检结果和最终确认三个位置明确提示“恢复将移除现有封面”，并展示受影响数量。
- [ ] 校正 AI 能力表达。当前“AI 生活观察员 / DAILY LIFE · AI”实际由浏览器内确定性规则生成，没有模型调用，也没有逐条事实来源。短期改名为“生活摘要”或“规则洞察”；真正接入 AI 后再补数据使用授权、事实/推断/建议区分、来源跳转、数据变化失效和模型不可用降级。
- [ ] 建立首个可回滚发布基线。完成首个 Git commit、版本号与变更记录，接入 CI，并形成数据库迁移、部署、升级、回滚和灾备恢复说明。
- [ ] 为核心业务闭环补浏览器级 E2E：新增/编辑/删除/恢复记录，记账后总览与时光档案同步，书影音封面上传，提醒触发，备份预检与恢复。现有 E2E 仅覆盖登录表单和移动导航。
- [ ] 补可靠的依赖安全扫描方案，不能把 `bun audit` 404 或未配置 scanner 当作“无漏洞”。

### P1：优先改善日常使用体验

- [ ] 将首页“记一笔、排日程、记体重、待买物品”改为抽屉或弹窗快捷记录，成功后局部刷新首页、来源模块和时光档案；当前操作会整页跳转。
- [ ] 优化桌面宽屏布局。当前主要内容集中在左侧约 40%，右侧长期大面积空白；为总览和数据页设计宽屏双栏/三栏布局，并统一合理的内容最大宽度。
- [ ] 修复习惯页在 1280 px 桌面视口下的轻微横向溢出，并继续检查各断点的表单、热力图和操作按钮布局。
- [ ] 补齐财务前端编辑能力。服务端已经支持账目更新，但 Web 当前只有新增与删除；同时补类型、分类、日期筛选、月度对比和与筛选口径一致的导出。
- [ ] 增强时光档案：关键词搜索、月份/日期范围导航、日期分组折叠、来源标签、可分享的月度/年度回顾；筛选状态同步 URL。
- [ ] 统一面向用户的领域文案，禁止直接展示 `food`、`transport` 等内部枚举值；统一日期、金额、重量、状态和分类格式。
- [ ] 增加草稿保护：自动保存表单草稿，或在有未保存修改时提供离开提醒；恢复后明确提示草稿是否已清除。
- [ ] 补齐表单可访问名称，重点检查 Select、DatePicker、文件选择和重复“删除/恢复”按钮，确保读屏用户能区分字段和对应记录。
- [ ] 明确提醒能力边界。当前仅在工作台打开时提供站内提醒，创建日程时需直接说明；系统级通知留到桌面端/平台能力阶段。
- [ ] 增加首次使用引导：用 3 分钟内完成第一条有效记录为目标，提供空状态任务、示例数据策略和可跳过的新手路径。

### P2：验证增长与长期价值

- [ ] 落地 PRD 已定义的产品指标：首次记录完成率、首页快捷入口转化率、周有效记录、覆盖模块数、7/30 日留存、创建失败率、导出/恢复成功率。个人版优先采用本地或明确授权的匿名统计。
- [ ] 增加全局搜索与统一标签，让账目、日程、购物、作品和未来旅行可以按主题关联，而不只按模块查找。
- [ ] 支持用户自定义首页卡片顺序、隐藏低频模块和选择重点目标，降低长期使用后的信息噪声。
- [ ] 继续优化资源体积：三套简体中文字体约 3.47 MB；按实际字重与字符覆盖做子集化，并持续观察首屏性能。

### 旅行记录模块（P1 后段）

产品定位：建议增加，但应定义为串联日程、财务、待买和时光档案的一段“生活项目”，而不是第七个完全独立的数据孤岛。

旅行 MVP：

- [ ] 旅行基础信息：名称、目的地、开始/结束日期、状态、封面和一句话期待。
- [ ] 行程视图：按天组织地点、活动和备注；日程记录可选关联 `tripId`，不复制维护第二套待办。
- [ ] 预算与花费：展示旅行预算、分类支出和预算差额；账目可选关联 `tripId`，仍以财务模块为唯一事实来源。
- [ ] 行李与采购：待买项可选关联 `tripId`，支持“准备中/已带/旅途中购买”等旅行视图状态，但不复制购物数据。
- [ ] 旅途记录：每日文字、地点和照片；旅行结束后生成路线、花费和回忆摘要，并自动投影到时光档案。
- [ ] 在现有日程、财务、待买、媒体/附件及时间线合约中设计可选旅行关联，保证删除、恢复、备份和并发控制行为一致。

旅行一期明确不做：多人协作、机酒预订、实时地图导航、公开游记社区、复杂路线优化和 AI 自动规划。先验证用户是否会完成“创建旅行 → 关联至少一条行程/账目 → 旅行结束后回顾”的闭环。

建议执行顺序：先完成 P0，再做快捷记录、宽屏布局、财务编辑和档案搜索，随后进入旅行 MVP；系统通知、全局搜索和个性化首页可与旅行二期统筹。

## 关键入口

- 共享财务合约：`packages/shared/src/schemas/finance.schema.ts`
- 财务仓储：`apps/server/src/repositories/finance.repository.ts`
- 财务接口：`apps/server/src/routes/finance/index.ts`
- 通用鉴权：`apps/server/src/security/authenticated-request.ts`
- Web 财务数据层：`apps/web/src/data-provider/finance.ts`
- Web 财务页：`apps/web/src/pages/finance/index.tsx`
- 生活模块共享合约：`packages/shared/src/schemas/life.schema.ts`
- 生活模块仓储：`apps/server/src/repositories/life.repository.ts`
- 生活模块接口：`apps/server/src/routes/life/index.ts`
- Web 生活模块数据层：`apps/web/src/data-provider/life.ts`
- Web 页面：`apps/web/src/pages/{timeline,habits,fitness,schedule,shopping,media}/index.tsx`
- 数据洞察页面：`apps/web/src/pages/{finance,habits,fitness,schedule}/index.tsx`
- 总览聚合仓储：`apps/server/src/repositories/life.repository.ts` 中的 `OverviewRepository`
- 总览与分页数据 Hook：`apps/web/src/data-provider/life.ts`
- 回收站仓储：`apps/server/src/repositories/trash.repository.ts`
- 通用删除确认：`apps/web/src/components/DeleteRecordButton.tsx`
- 最近删除页面：`apps/web/src/pages/settings/index.tsx`
- 全局错误入口：`apps/web/src/contexts/AppProviders.tsx`、`apps/web/src/data-provider/error-toast.ts`
- 提醒监听器：`apps/web/src/components/ReminderWatcher.tsx`
- 到期查询与轮询：`apps/server/src/repositories/life.repository.ts` 中的 `TodoRepository.getDueReminders`、`apps/web/src/data-provider/life.ts` 中的 `useDueReminders`
- 备份共享合约：`packages/shared/src/schemas/backup.schema.ts`
- 备份服务：`apps/server/src/services/data-backup.service.ts`
- Excel 工作簿生成与读取：`apps/server/src/services/excel-backup.service.ts`
- 数据安全接口：`apps/server/src/routes/data/index.ts`
- Web 备份数据层与界面：`apps/web/src/data-provider/backup.ts`、`apps/web/src/pages/settings/index.tsx`
- 封面资源服务：`apps/server/src/services/media-cover.service.ts`
- 封面上传与读取接口：`apps/server/src/routes/media-assets/index.ts`
- 书影音封面界面：`apps/web/src/pages/media/index.tsx`

## 已验证流程

自动化集成测试已覆盖六类生活记录的写入、状态流转、统计、统一时间线投影、游标翻页、总览聚合，以及五类生活记录的编辑、陈旧版本拒绝、软删除和恢复；习惯接口额外校验 30 天逐日分析结果。日程测试覆盖提醒创建、无时间提醒拒绝、到期查询和完成后不再提醒。备份测试覆盖 Excel 文件生成、八个工作表结构、隐藏恢复工作表、预检、篡改拒绝、事务恢复、恢复后数据校验和 Session 保留。封面测试覆盖真实图片压缩、鉴权读取、替换旧资源、移除文件和损坏图片拒绝。统一响应测试覆盖成功、失败、非 2xx、空数据和非法顶层结构。财务分类、体重趋势和周视图均从现有响应派生，不新增请求。真实浏览器已验证空登录提交会显示 Toast 且页面内错误块数量为 0；此前也已验证五个编辑页面、删除确认弹窗、取消删除和“最近删除”空状态，未改动正式数据；移动端 390×844 下全部业务页面无横向溢出。2026-09-03 当前验证结果：`bun run check` 全量通过，15 个测试文件、38 个测试通过，三端类型检查和生产构建通过；`bun run test:e2e` 为 3 个通过、1 个按桌面/移动项目条件跳过。

## 下一阶段建议

优先进入“产品信任与使用效率补全”阶段，具体任务以“产品经理审判与补全清单”为准：先完成备份封面风险、AI 能力表达、发布基线和核心业务 E2E，再处理快捷记录、宽屏布局、财务编辑、档案搜索与旅行 MVP。系统级通知留待桌面端平台能力阶段。

## 注意事项

- Bun 继续负责包管理与顶层任务；服务端运行和迁移使用 Node/tsx，以规避当前 Bun 与 `better-sqlite3` 的 N-API 崩溃。
- `animal-island-ui/style` 只能在 Web 入口导入一次；业务页面只从包根导入组件。
- Web 生产构建主入口当前约 459.79 kB（gzip 152.50 kB），已经低于 Vite 500 kB 提示线；三套简体中文字体仍合计约 3.47 MB，后续优先做字体子集化。
- Web 一期按技术方案采用“应用打开时提醒”，不是操作系统级后台通知；浏览器或桌面应用完全关闭时不会触发。
- Excel 恢复只覆盖当前管理员的业务数据，不恢复密码、Session、AI 密钥或资源文件；服务端内部的恢复前灾备快照仍以 JSON 写入 `BACKUP_DIRECTORY`，不需要用户操作。
- 封面文件默认写入 `UPLOAD_DIR`；Excel 业务备份不包含封面二进制文件，执行整体恢复时会清理原有封面资源。
- 不要把依赖审计的 404 或未配置 scanner 解读为“零漏洞”；当前依赖安全状态仍待补充验证。
- 仓库尚无首个 Git commit；现有文件均可能显示为未跟踪，不要误删。

## Suggested skills

- `animal-island-ui-style`：继续开发或调整 Web 页面时使用。
- `vercel-react-best-practices`：处理路由懒加载、查询缓存和主包体积时使用。
- `code-review-and-quality`：准备首个提交或合并前使用。
